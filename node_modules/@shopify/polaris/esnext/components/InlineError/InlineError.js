import * as React from 'react';
import Icon from '../Icon';
import styles from './InlineError.scss';
function InlineError({ message, fieldID }) {
    if (!message) {
        return null;
    }
    return (<div id={`${fieldID}Error`} className={styles.InlineError}>
      <div className={styles.Icon}>
        <Icon source="alert"/>
      </div>
      {message}
    </div>);
}
export default InlineError;
